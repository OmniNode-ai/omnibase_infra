---
# Single mixin test - MixinHealthCheck only
node_id: "health_check_effect"
node_type: "effect"
version: "v1_0_0"
namespace: "omninode.services.test"

metadata:
  name: "Health Check Effect Node"
  description: "Effect node with health check mixin only"
  author: "Test Generator"
  tags: ["test", "health", "single-mixin"]

mixin_configuration:
  mixins:
    - mixin_name: "MixinHealthCheck"
      config:
        health_check_interval_seconds: 30
        critical_checks:
          - "database_connection"
          - "external_api"

input_schema:
  type: "object"
  properties:
    data:
      type: "string"

output_schema:
  type: "object"
  properties:
    result:
      type: "string"
