# SPDX-FileCopyrightText: 2025 OmniNode.ai Inc.
# SPDX-License-Identifier: MIT
#
# OMN-17886 AC4: the findings a from-empty build of the forward-migration corpus
# still produces against the declared omninode_internal topology, and the step
# that removes each one.
#
# SHRINK-ONLY. tests/integration/migrations/test_omninode_internal_live_acl_gate_omn17886.py
# builds omnidash_analytics from empty with scripts/run-forward-migrations.sh, runs
# scripts/validation/check_omninode_internal_live_acl.py against it, and fails in
# both directions:
#   * a finding that is not listed here fails the gate (a migration conferred or
#     omitted a grant the topology does not declare);
#   * an entry here that no longer reproduces fails the gate too, so the entry is
#     deleted in the same change that fixes it and this list only ever shrinks.
#
# Nothing may be added to this list to make the gate pass. A new finding is fixed
# by migration or by a declaration, never by listing it.
#
# Measured 2026-09-24 on a fresh PostgreSQL 16 cluster, profile onex-dev: 20 findings.
entries:
  # AC2 step 1: node_gateway_link_health_write_effect declares no db_io for these.
  - finding: UNDECLARED_GRANT omninode_runtime INSERT ON omninode_internal.gateway_link_health
    removed_by: OMN-17886 AC2 step 1 (db_io for node_gateway_link_health_write_effect)
  - finding: UNDECLARED_GRANT omninode_runtime SELECT ON omninode_internal.gateway_link_health
    removed_by: OMN-17886 AC2 step 1 (db_io for node_gateway_link_health_write_effect)
  - finding: UNDECLARED_GRANT omninode_runtime UPDATE ON omninode_internal.gateway_link_health
    removed_by: OMN-17886 AC2 step 1 (db_io for node_gateway_link_health_write_effect)
  - finding: UNDECLARED_GRANT omninode_runtime INSERT ON omninode_internal.gateway_link_health_status
    removed_by: OMN-17886 AC2 step 1 (db_io for node_gateway_link_health_write_effect)
  - finding: UNDECLARED_GRANT omninode_runtime SELECT ON omninode_internal.gateway_link_health_status
    removed_by: OMN-17886 AC2 step 1 (db_io for node_gateway_link_health_write_effect)
  - finding: UNDECLARED_GRANT omninode_runtime UPDATE ON omninode_internal.gateway_link_health_status
    removed_by: OMN-17886 AC2 step 1 (db_io for node_gateway_link_health_write_effect)
  # AC2 step 1: node_projection_runner_fleet deletes; `access: read_write` cannot say so yet.
  - finding: UNDECLARED_GRANT omninode_runtime DELETE ON omninode_internal.runner_fleet_liveness
    removed_by: OMN-17886 AC2 step 1 (a DELETE declaration for runner_fleet_liveness)
  # AC2 step 1: node_savings_estimation_compute declares no db_io at all.
  - finding: UNDECLARED_GRANT omninode_runtime INSERT ON omninode_internal.savings_correlation_finalizations
    removed_by: OMN-17886 AC2 step 1 (db_io for node_savings_estimation_compute)
  - finding: UNDECLARED_GRANT omninode_runtime SELECT ON omninode_internal.savings_correlation_finalizations
    removed_by: OMN-17886 AC2 step 1 (db_io for node_savings_estimation_compute)
  - finding: UNDECLARED_GRANT omninode_runtime UPDATE ON omninode_internal.savings_correlation_finalizations
    removed_by: OMN-17886 AC2 step 1 (db_io for node_savings_estimation_compute)
  - finding: UNDECLARED_GRANT omninode_runtime INSERT ON omninode_internal.savings_injection_signals
    removed_by: OMN-17886 AC2 step 1 (db_io for node_savings_estimation_compute)
  - finding: UNDECLARED_GRANT omninode_runtime SELECT ON omninode_internal.savings_injection_signals
    removed_by: OMN-17886 AC2 step 1 (db_io for node_savings_estimation_compute)
  - finding: UNDECLARED_GRANT omninode_runtime UPDATE ON omninode_internal.savings_injection_signals
    removed_by: OMN-17886 AC2 step 1 (db_io for node_savings_estimation_compute)
  - finding: UNDECLARED_GRANT omninode_runtime INSERT ON omninode_internal.savings_validator_catch_signals
    removed_by: OMN-17886 AC2 step 1 (db_io for node_savings_estimation_compute)
  - finding: UNDECLARED_GRANT omninode_runtime SELECT ON omninode_internal.savings_validator_catch_signals
    removed_by: OMN-17886 AC2 step 1 (db_io for node_savings_estimation_compute)
  - finding: UNDECLARED_GRANT omninode_runtime UPDATE ON omninode_internal.savings_validator_catch_signals
    removed_by: OMN-17886 AC2 step 1 (db_io for node_savings_estimation_compute)
  # AC2 step 2: the explicit-grant migration delivers the declared schema USAGE.
  - finding: MISSING_DECLARED_SCHEMA_GRANT validator_ro USAGE ON SCHEMA omninode_internal
    removed_by: OMN-17886 AC2 step 2 (explicit-grant migration)
  # AC2 step 4: the default-privilege rules go last, after steps 1-3 (Jonah, 2026-09-24).
  - finding: UNDECLARED_DEFAULT_PRIVILEGE owner=postgres objtype=r omninode_runtime INSERT IN SCHEMA omninode_internal
    removed_by: OMN-17886 AC2 step 4 (drop the default-privilege rules)
  - finding: UNDECLARED_DEFAULT_PRIVILEGE owner=postgres objtype=r omninode_runtime SELECT IN SCHEMA omninode_internal
    removed_by: OMN-17886 AC2 step 4 (drop the default-privilege rules)
  - finding: UNDECLARED_DEFAULT_PRIVILEGE owner=postgres objtype=r omninode_runtime UPDATE IN SCHEMA omninode_internal
    removed_by: OMN-17886 AC2 step 4 (drop the default-privilege rules)
