# Recorded lab executions of down-migrations (OMN-19344; plan row R7, rule RB-2).
#
# A forward-only or contract migration is a rollback barrier: no composition
# older than it is an eligible rollback candidate. The ONE exception is a paired
# down-migration that has itself been executed on the lab, recorded here, bound
# to the migration id and to the sha256 of both scripts. A down-script existing
# on disk lifts nothing. Editing either script voids its record, and
# scripts/validation/check_migration_class.py fails CI until the run is repeated.
#
# Written only by scripts/migrations/record_down_migration_execution.py, which
# applies forward -> down -> forward in a fresh database it creates and drops,
# and records PASS only when the down restored the pre-forward catalog exactly.
schema_version: 1
executions:
  - migration: forward/031_create_llm_call_metrics_and_cost_aggregates.sql
    down_script: rollback/rollback_031_llm_call_metrics_and_cost_aggregates.sql
    forward_sha256: da63eaf05f9c0036cc44ef036925997b88e463d20a6027ac2899589153db56f8
    down_sha256: d58b1ae6e4f6437b55818719f34c1685222c17c9f24e1fe6b74bb814d0f3287e
    surface: mac-scratch:wr-recovery
    database: downproof_20260924012700 on container wr-recovery-scratchdb (created and dropped by this run)
    executed_at: '2026-09-24T01:27:02Z'
    outcome: PASS
    evidence: 'forward exit 0; down exit 0; forward re-apply exit 0; catalog objects before=7 after-forward=54 after-down=7 after-reforward=54; down restored the pre-forward catalog exactly; prerequisites: forward/029_create_db_metadata.sql'
