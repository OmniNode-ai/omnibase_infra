## Acceptance criteria

- [X] **AC1 **~~An explicit ~~`~~--bus~~` ~~and an explicit ~~`~~--kafka-bootstrap~~` ~~each emit a provenance line naming themselves as an OVERRIDE of the resolved transport~~ **CLOSED**
- [X] **AC2** ~~The transport-authority fork is ruled on in writing before any tier-3 wiring lands.~~ **RULED 2026-09-08 — CLOSED.**
- [X] **AC3 **~~Once AC2 is ruled: `onex delegate` passes `config_bus=` from the ruled surface~~ **CLOSED**
- [ ] **AC4** Once AC2 + OMN-17295 land: with a configured shared bus, the CLI publishes and does **not** subscribe the entry handler. Locus proof is the OMN-16932 in-lane recipe: fresh correlation id, `docker logs omninode-runtime{,-effects} --since 20m | grep <corr>` returns **non-zero** hits, and the Mac CLI's own consumer group shows **no** committed-offset movement on the command topic for that run.
- [ ] **AC5** The stale Mac CLI group `local.omnibase_core.handlerdelegateskill.consume.v1.__t.…` (committed 53 / lag 93) is reaped as part of AC4, or it replays 93 old commands the first time anything re-hosts it.
- [X] **AC6** `~~runtime_local.py:1288-1296~~`~~ no longer swallows the frozen-model ValidationError into a pass~~ **CLOSED**
