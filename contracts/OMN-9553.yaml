schema_version: "1.0.0"
ticket_id: OMN-9553
title: "UnknownTopicOrPartitionError must not trip Kafka circuit breaker"
summary: "Treat missing Kafka topics as configuration errors instead of broker connectivity failures so one missing topic cannot open the circuit breaker for healthy topics."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: "Focused event-bus and crash-loop tests pass."
    command: "uv run pytest tests/unit/event_bus/test_circuit_breaker_topic_error.py tests/integration/test_runtime_crash_loop_fix.py -q"
  - kind: manual
    description: "Post-merge runtime validation must confirm clean import and stable RestartCount."
    command: "deploy: docker exec omninode-runtime python - <<'PY'\nfrom omnibase_infra.event_bus.event_bus_kafka import EventBusKafka\nprint(EventBusKafka.__name__)\nPY"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "UnknownTopicOrPartitionError is handled before KafkaError and does not increment circuit-breaker failures."
    source: manual
    checks:
      - check_type: command
        check_value: "uv run pytest tests/unit/event_bus/test_circuit_breaker_topic_error.py tests/integration/test_runtime_crash_loop_fix.py -q"
  - id: dod-deploy
    description: "deploy gate evidence: PR body records post-merge runtime validation; pre-merge live RestartCount proof is not claimed."
    source: manual
    checks:
      - check_type: command
        check_value: "deploy: docker exec omninode-runtime python - <<'PY'\nfrom omnibase_infra.event_bus.event_bus_kafka import EventBusKafka\nprint(EventBusKafka.__name__)\nPY"
