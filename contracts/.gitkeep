# This file ensures the contracts directory is tracked by git.
# The contracts directory contains runtime configuration files:
#   - runtime/runtime_config.yaml - Runtime kernel configuration
#
# See knowledge-base-internal:runbooks/omnibase-infra-docker-deployment.md for how contracts are used in Docker deployment.
