schema_version: "1.0.0"
ticket_id: "OMN-10923"
title: "Delegation profile config loader"
summary: "Load delegation runtime profile configuration from contract YAML instead of environment variables."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-unit-loader"
    description: "Unit coverage proves the loader reads typed contract configuration and does not read delegation env vars."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/unit/runtime/test_delegation_profile_config_loader.py -q"
  - id: "dod-integration-loader"
    description: "Integration coverage exists for delegation runtime profile projection."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/integration/runtime/test_delegation_profile_config_loader_integration.py -q"
  - id: "dod-deploy"
    description: "Deploy validation plan for the runtime config loader; no live runtime restart was performed in this PR."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "deploy: after merge, verify the runtime container can import DelegationProfileConfigLoader and load the deployed delegation runtime profile contract without env var fallbacks"
