# OMN-9637 contract file for omnibase_infra deploy-gate (OMN-8912).
# The canonical ticket contract lives in onex_change_control.
# This file is the per-repo deploy-evidence carrier only.
schema_version: '1.0.0'
ticket_id: OMN-9637
summary: 'Task P16: Add node_delegation_orchestrator dispatch resolver'
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - topics
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: PR opened for delegation dispatch resolver runtime path
    source: generated
    checks:
      - check_type: command
        check_value: 'gh pr view 1420 --repo OmniNode-ai/omnibase_infra --json state -q .state'
  - id: dod-002
    description: Contract artifact file present in repo
    source: generated
    checks:
      - check_type: command
        check_value: 'test -f contracts/OMN-9637.yaml'
  - id: dod-003
    description: Runtime deploy verifies dispatch resolver routes AGENT invocations to remote agent effect
    source: manual
    checks:
      - check_type: command
        check_value: 'docker exec omninode-runtime python -c "from omnibase_infra.nodes.node_delegation_orchestrator.dispatch_resolver import resolve_effect_topic; from omnibase_core.enums.enum_invocation_kind import EnumInvocationKind; assert resolve_effect_topic(EnumInvocationKind.AGENT)"'
