schema_version: '1.0.0'
ticket_id: OMN-9905
title: 'Overlay loader and merge semantics for plugin scope'
summary: 'Implement kustomize-style ONEX plugin enforcement-scope overlay loading.'
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - public_api
  - protocols
evidence_requirements:
  - kind: tests
    description: Scope loader unit and integration tests pass
    command: uv run pytest tests/unit/scope/test_loader.py tests/integration/scope/test_loader_integration.py -q
  - kind: ci
    description: CI pipeline green on PR
    command: gh pr checks 1503 --repo OmniNode-ai/omnibase_infra
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: Scope overlay loader unit tests prove selector, merge, disabled, and cache semantics
    source: generated
    checks:
      - check_type: command
        check_value: 'uv run pytest tests/unit/scope/test_loader.py -q'
  - id: dod-002
    description: Scope overlay loader integration test proves file-backed overlay behavior
    source: generated
    checks:
      - check_type: command
        check_value: 'uv run pytest tests/integration/scope/test_loader_integration.py -q'
  - id: dod-deploy
    description: Runtime deploy preflight covers local overlay loading without requiring live runtime restart
    source: generated
    checks:
      - check_type: command
        check_value: 'uv run pytest tests/integration/scope/test_loader_integration.py tests/ci/test_env_parity.py -q'
