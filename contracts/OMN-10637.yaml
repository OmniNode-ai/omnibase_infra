# OMN-10637 contract file for omnibase_infra deploy-gate (OMN-8912).
# Canonical ticket contract lives in onex_change_control (OCC PR #758, merged
# at e01a63ae4aa4edcf474b665859a217cf52915ddf). This repo-local contract carries
# deploy-gate evidence for the Bifrost delegation routing config.
schema_version: '1.0.0'
ticket_id: OMN-10637
summary: 'Bifrost delegation routing rules config + Pydantic schema + loader for task classes'
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-deploy
    description: >-
      deploy-gate: bifrost_delegation.yaml + model_bifrost_delegation_config.py + config_loader_bifrost_delegation.py are dormant — none are imported by any runtime path yet. The loader will be wired in a follow-up ticket once the LLM inference effect handler consumes routing rules. No omninode-runtime container behaviour changes from this PR; no Docker restart required. Verified: `grep -r "config_loader_bifrost_delegation\|model_bifrost_delegation_config" src/` returns only the new files themselves and the test/allowlist files.
    source: manual
    checks:
      - check_type: command
        check_value: >-
          deploy omninode-runtime not required: bifrost delegation loader and model are dormant config-only additions. No handler imports them yet; running containers are unaffected. Wiring will land in a follow-up ticket.
