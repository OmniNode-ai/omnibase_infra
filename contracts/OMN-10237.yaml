# OMN-10237 contract file for omnibase_infra deploy-gate (OMN-8912).
# The canonical ticket contract lives in onex_change_control.
# This file is the per-repo deploy-evidence carrier only.
schema_version: "1.0.0"
ticket_id: "OMN-10237"
summary: "refactor(OMN-10237): collapse local ingress into broker passthrough"
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - protocols
evidence_requirements:
  - kind: "tests"
    description: "Local runtime ingress unit tests pass"
    command: "uv run pytest tests/unit/runtime/ -q"
  - kind: "manual"
    description: "Runtime deploy validates collapsed ingress after merge"
    command: "deploy omninode-runtime after merge and verify ingress routes through broker passthrough without errors"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Local runtime ingress unit tests pass"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/unit/runtime/ -q"
  - id: "dod-002"
    description: "ONEX validators pass with command_name exemption"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/unit/contracts/ -q"
  - id: "dod-003"
    description: "Runtime deploy validates collapsed ingress after merge"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "deploy omninode-runtime after merge and verify ingress routes through broker passthrough without errors"
