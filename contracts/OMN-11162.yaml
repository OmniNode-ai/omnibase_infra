# OMN-11162 contract file for omnibase_infra deploy-gate.
# Canonical ticket evidence lives in onex_change_control.
schema_version: "1.0.0"
ticket_id: "OMN-11162"
title: "Flip omnibase_infra contract path preflight to blocking"
summary: >
  Converts contract path preflight dead local module references from warnings into blocking errors. The touched registry effect contract dependency metadata points at existing protocol modules.

is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-deploy"
    description: "CI/contract metadata hardening; no live deploy, restart, broker topic, container image, or runtime mutation is required."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "echo 'deploy-gate: OMN-11162 changes CI contract-path enforcement and dependency metadata only; no docker exec, rpk topic produce, live deploy, restart, or runtime mutation required before merge'"
