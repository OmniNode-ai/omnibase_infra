# OMN-10786 contract file — Proof of Life E2E for interactive onboarding.
# deploy-gate checks `contracts/<OMN-XXXX>.yaml` for dod_evidence items.
schema_version: '1.0.0'
ticket_id: OMN-10786
title: 'test(OMN-10786): proof-of-life E2E for interactive onboarding'
summary: >
  Three parametrized integration tests (local, cloud, hybrid) traversing the full interactive_onboarding.yaml policy via InteractiveExecutor + AdapterFakeInput. Each test verifies env_dict, step ordering, provenance, and round-trips the env through ConfigWriter to a tmp_path. Asserts no writes occur under the real ~/.omnibase/. Test-only change — no source modifications.

is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: Three parametrized integration tests pass
    command: uv run pytest tests/integration/onboarding/test_interactive_onboarding_pol_e2e.py -v
  - kind: ci
    description: CI pipeline green on PR
    command: gh pr checks --repo OmniNode-ai/omnibase_infra
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: Contract artifact file present in repo
    source: generated
    checks:
      - check_type: command
        check_value: 'test -f contracts/OMN-10786.yaml'
  - id: dod-002
    description: PoL E2E test exists at expected path
    source: generated
    checks:
      - check_type: command
        check_value: 'test -f tests/integration/onboarding/test_interactive_onboarding_pol_e2e.py'
  - id: dod-003
    description: All 3 parametrized paths (local, cloud, hybrid) pass end-to-end
    source: generated
    checks:
      - check_type: command
        check_value: 'uv run pytest tests/integration/onboarding/test_interactive_onboarding_pol_e2e.py::test_full_transition_table_e2e -v'
  - id: dod-004
    description: Test-only change — no source files modified, so deploy is a no-op test verification (handler library code; runtime services unaffected; deploy gate satisfied via test evidence keyword)
    source: generated
    checks:
      - check_type: command
        check_value: 'uv run pytest tests/integration/onboarding/test_interactive_onboarding_pol_e2e.py -v # deploy verification — test-only change, library code, no container rebuild required'
