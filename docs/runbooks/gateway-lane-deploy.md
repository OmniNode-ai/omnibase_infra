# Gateway lane deploy (`omninode-gateway` compose project on `<onex-host>`)

Migrated to the OmniNode knowledge base as `runbooks/gateway-lane-deploy.md`.

Full documentation → https://github.com/OmniNode-ai/knowledge-base
