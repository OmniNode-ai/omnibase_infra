name: reviewdog
on:
  pull_request:
    types: [opened, synchronize, reopened]
  merge_group:

jobs:
  reviewdog:
    uses: OmniNode-ai/omniclaude/.github/workflows/reviewdog-review.yml@f0892d9cb908b0eaa44a0c5a89dc4b37b27a8b19
    with:
      src-dir: "src/"
      ruff-enabled: true
      mypy-enabled: true
      security-enabled: true
      fail-level: "error"
    secrets:
      REVIEWDOG_PAT: ${{ secrets.REVIEWDOG_PAT }}
